

```bash
source /opt/ros/noetic/setup.bash
# activate a dedicated conda environment (base was python-3.6)
# (good thig - optional:) pip install --extra-index-url https://rospypi.github.io/simple/ rospy rosbag
pip install PyYAML
pip install rospkg
pip3 install pycryptodomex
# then you should install gnupg
# then ... 
```
